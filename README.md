📦 سیستم مدیریت انبارداری هوشمند (WMS)

🌟 معرفی پروژه

این سیستم یک پلتفرم پیشرفته و بلید محور برای مدیریت جامع موجودی و عملیات انبارداری است. این سیستم با استفاده از پشته فناوری‌های مدرن Laravel و Livewire توسعه یافته است تا یک رابط کاربری واکنش‌گرا، سریع و کارآمد را برای ردیابی دقیق کالاها، مدیریت مکان‌ها و گزارش‌گیری فراهم کند.

تمرکز اصلی این سامانه بر مکانیزه کردن فرآیندهای فیزیکی انبار از طریق استفاده از تکنولوژی QR Code و همچنین ارائه ابزارهای گزارش‌گیری قوی با قابلیت خروجی Excel است.

🎯 قابلیت‌های کلیدی

مدیریت موجودی بلادرنگ: ردیابی لحظه‌ای ورودی و خروجی کالاها (Inbound/Outbound).

شناسایی سریع کالا: تولید و اسکن کدهای QR برای شناسایی سریع محصولات و موقعیت‌های انبار.

گزارش‌های ساختاریافته: قابلیت تولید گزارش‌های دقیق موجودی و تراکنش‌ها در فرمت‌های پرکاربرد اکسل.

رابط کاربری پویا (SPA-like): تجربه کاربری سریع و بدون بارگذاری مجدد صفحه به لطف Livewire.

سیستم احراز هویت امن: محافظت از سیستم با استفاده از مکانیزم‌های احراز هویت استاندارد لاراول.

🏗️ معماری فناوری (Tech Stack)

این پروژه بر پایه فریمورک Laravel و با استفاده از Blade به عنوان موتور قالب‌سازی اصلی ساخته شده است.

بخش

فناوری/پکیج

هدف در پروژه

Backend Core

Laravel (Blade Centric)

پیاده‌سازی منطق سرور، ORM (Eloquent)، و مدیریت دیتابیس.

Frontend Dynamics

livewire/livewire

ایجاد کامپوننت‌های پویا، مدیریت وضعیت (State Management)، و ارتباط دوطرفه بین Frontend و Backend. این پکیج تجربه کاربری مشابه Single Page Application (SPA) را بدون پیچیدگی‌های فریمورک‌های جاوا اسکریپت فراهم می‌کند.

Barcode/QR Code

simplesoftwareio/simple-qrcode

تولد کدهای QR با کیفیت بالا برای هر کالای منحصر به فرد (SKU) و نیز برچسب‌گذاری محل‌های انبار (Location Tags).

Excel I/O

maatwebsite/excel

مدیریت عملیات واردات (Import) و صادرات (Export) داده‌های حجیم موجودی و تاریخچه تراکنش‌ها در قالب‌های XLSX و CSV. این قابلیت برای گزارش‌گیری و بررسی‌های دوره‌ای حیاتی است.

Styling

Tailwind CSS

طراحی رابط کاربری واکنش‌گرا، سریع و مدرن.

Authentication

Laravel Breeze / Fortify

پیاده‌سازی سریع، استاندارد و امن فرآیندهای ورود، ثبت‌نام و بازیابی رمز عبور.

⚙️ راهنمای نصب و راه‌اندازی

۱. پیش‌نیازها

PHP >= 8.1

Composer

Node.js (npm / Yarn)

یک سرور دیتابیس (مانند MySQL یا PostgreSQL)

۲. مراحل نصب

Clone کردن مخزن (Repository):

git clone [لینک مخزن شما]
cd [نام پروژه شما]


نصب وابستگی‌های PHP:

composer install


تنظیمات محیط:

فایل .env.example را به .env کپی کنید و اطلاعات دیتابیس را در آن تنظیم نمایید.

cp .env.example .env
php artisan key:generate


اجرای Migration دیتابیس:

php artisan migrate
# در صورت نیاز به داده‌های اولیه: php artisan migrate --seed


نصب و کامپایل وابستگی‌های فرانت‌اند:

npm install
npm run dev  # برای توسعه
# npm run build  # برای محیط Production


اجرای سرور توسعه:

php artisan serve


🛡️ ملاحظات امنیتی و توسعه امن

به عنوان یک سیستم حساس در حوزه مدیریت داده‌های تجاری، امنیت در هسته این سامانه قرار دارد. رویکردهای امنیتی زیر در توسعه رعایت شده است:

۱. اعتبارسنجی و پاکسازی داده‌ها (Data Sanitization & Validation)

کلیه ورودی‌ها، به‌خصوص ورودی‌هایی که برای تولید QR Code یا درج در گزارش‌های اکسل استفاده می‌شوند، به شدت اعتبارسنجی می‌شوند.

محافظت در برابر XSS: استفاده از Blade و Livewire (که به طور پیش‌فرض از خروجی‌های خودکار فرار می‌کنند) برای جلوگیری از حملات Cross-Site Scripting.

استفاده از Facadeهای امن: تمام عملیات دیتابیس از طریق Eloquent ORM انجام می‌شود تا خطر SQL Injection به حداقل برسد.

۲. کنترل دسترسی (Authorization)

به جای تکیه صرف بر auth Middleware، از Laravel Policies و Gates برای اجرای قوانین دقیق دسترسی (مانند "فقط مدیر انبار می‌تواند موجودی را ویرایش کند") استفاده شده است تا اصل حداقل امتیاز (Principle of Least Privilege) رعایت شود.

۳. مدیریت QR Code

هنگام تولید کدهای QR، از simplesoftwareio/simple-qrcode برای ارائه خروجی‌های SVG یا PNG ایمن استفاده می‌شود، و محتوای رمزگذاری شده در QR Code ابتدا از نظر امنیتی بررسی می‌شود تا از تزریق لینک‌های مخرب یا داده‌های غیرمجاز جلوگیری شود.

🤝 مشارکت در توسعه

از هرگونه مشارکت و بازخورد استقبال می‌شود. لطفاً برای گزارش باگ‌ها یا پیشنهاد ویژگی‌های جدید، یک Issue در GitHub ایجاد کنید یا مراحل زیر را دنبال نمایید:

Fork کردن مخزن.

ایجاد Branch جدید (git checkout -b feature/NewFeature).

Commit تغییرات خود.

ارسال Pull Request به Branch اصلی.

📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.
